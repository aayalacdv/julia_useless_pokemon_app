import { Request, Response, NextFunction } from 'express';



const middlewareJulia = (schema : any) => async (req: Request, res: Response, next : NextFunction) => {
    //logica de validacion de la schema zod joy jup

}


